import { useBackend, useLocalState } from "../backend";
import { Box } from '../../components';

export const QuirkPicker = (props, context) => {
  const { act, data } = useBackend(context);

  return <Box>

  </Box>
};

export default QuirkPicker;
